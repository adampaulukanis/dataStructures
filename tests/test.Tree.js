/* global describe, it */
'use strict';

const assert = require('assert');
const { Tree, Node } = require('..');

describe('testing Tree', function () {
	it('Traverses the tree with depth-first search',);

	it('Traverses the tree with breadth-first search');

	it('adds');

	describe('removing nodes', function(){
		it('removes node');

		it('throws when parent does not exist');

		it('throws when node to remove does not exist');
	});
});
